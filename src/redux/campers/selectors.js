export const selectCampers = (state) => state.campers.items;
export const selectTotalNumberCampers = (state) => state.campers.totalNumberCampers;

export const selectVehicleCard = (state) => state.campers?.vehicleCard;
